const space = {
    flex: "auto"
}

const verticalCenter = {
    marginTop: "auto",
    marginBottom: "auto"

}

const logoStyle = {
    fontWeight: 'bold', 
    fontSize: "x-large", 
    color: "#ffffffdb"
}

export { space, verticalCenter, logoStyle}